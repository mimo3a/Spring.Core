<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:context="http://www.springframework.org/schema/context"
    xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">

    <context:property-placeholder location="classpath:musicPlayer.properties"/>

    <bean id="classicMusicBean"
     class="ua.bis.mimoza.spring.ClassicMusic" 
     init-method="doMyInit" 
     destroy-method="doMyDestroy" 
     factory-method="getClassicMusic">
     
        <property name="song" value="Classical Song"/>
    </bean>

    <bean id="rockMusicBean" 
    class="ua.bis.mimoza.spring.RockMusic">
        <property name="song" value="Rock Song"/>
    </bean>

    <bean id="rapMusicBean" 
    class="ua.bis.mimoza.spring.RapMusic">
        <property name="song" value="Rap Song"/>
    </bean>

    <bean id="musicPlayerBean" 
    class="ua.bis.mimoza.spring.MusicPlayer" 
    scope = "prototype" 
    init-method="myInit" 
    destroy-method="myDestroy">
        <property name="musicList">
            <list>
                <ref bean="rockMusicBean"/>
                <ref bean="classicMusicBean"/>
                <ref bean="rapMusicBean"/>
            </list>
        </property>
        <property name="name">
        	<value>${musicPlayer.name}</value>
        </property>	
        <property name="volume" value="${musicPlayer.volume}" />
    </bean>
</beans>
